# eosal-defs-espidf.txt - Set cmake build environment variables for eosal ESP-IDF project.
# 1.9.2021/pekka

# ???????????????????????????????????????????????????????????????
# set(SDKCONFIG $ENV{E_ROOT}/eosal/osbuild/esp-idf/menuconfig/sdkconfig)

# Generic target operating system name "esp32". Used as folder name for OS specific code.
if (NOT DEFINED ENV{E_OS})
set(ENV{E_OS} "esp32")
endif()

# Target operating system version "espidf" is used as name for binary folders and as part of
# the installation package name.
if (NOT DEFINED ENV{E_OSVER})
set(ENV{E_OSVER} ${IDF_TARGET})
endif()

# Compiler name, used as part of library and temporary folder name.
if (NOT DEFINED ENV{E_COMPILER})
set(ENV{E_COMPILER} "xtensa")
endif()

if (NOT DEFINED ENV{E_ARCH})
set(ENV{E_ARCH} ${IDF_TARGET})
endif()

# Postfix letter "d" for debug libraries and binaries.
# if (CMAKE_BUILD_TYPE MATCHES "(Debug)")
#  set(E_POSTFIX "d")
#else()
#  set(E_POSTFIX "")
# endif()

# List components which are typically required by ESP32 binary.
set(E_REQUIRES "eosal_esp32")
list(APPEND E_REQUIRES "pins_esp32")
list(APPEND E_REQUIRES "iocom_esp32")
list(APPEND E_REQUIRES "nodeconf_esp32")
list(APPEND E_REQUIRES "deviceinfo_esp32")
list(APPEND E_REQUIRES "lighthouse_esp32")
list(APPEND E_REQUIRES "devicedir_esp32")
list(APPEND E_REQUIRES "gazerbeam_esp32")

# List typical include directories.  
set(E_INCLUDE_DIRS ".")
list(APPEND E_INCLUDE_DIRS "$ENV{E_ROOT}/eosal")
list(APPEND E_INCLUDE_DIRS "$ENV{E_ROOT}/pins")
list(APPEND E_INCLUDE_DIRS "$ENV{E_ROOT}/iocom")
list(APPEND E_INCLUDE_DIRS "$ENV{E_ROOT}/iocom/extensions/nodeconf")
list(APPEND E_INCLUDE_DIRS "$ENV{E_ROOT}/iocom/extensions/deviceinfo")
list(APPEND E_INCLUDE_DIRS "$ENV{E_ROOT}/iocom/extensions/lighthouse")
list(APPEND E_INCLUDE_DIRS "$ENV{E_ROOT}/iocom/extensions/devicedir")
list(APPEND E_INCLUDE_DIRS "$ENV{E_ROOT}/iocom/extensions/gazerbeam")
